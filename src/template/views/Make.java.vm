package $scprm.packageName;

import creamy.activity.AvailableActivity;
import javafx.fxml.FXML;
import models.$scprm.modelName;

public class Make extends AvailableActivity {
    public void cfinitialize() {
        // Write initialize codes.
    }
}